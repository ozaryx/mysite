new WOW().init();

const mySwiper = new Swiper('.swiper-container', {
    scrollbar: {
      el: '.swiper-scrollbar',
      draggable: true,
    },
  });

// Now you can use all slider methods like
// mySwiper.slideNext();
